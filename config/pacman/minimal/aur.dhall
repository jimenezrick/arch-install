-- We need `nmtree` to restore `.git` permissions in /etc and also
-- we install `libnbcompat` which is just a build dependency which
-- simplifies AUR dependency management
[ "libnbcompat", "nmtree" ]
